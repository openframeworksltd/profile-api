# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

# The javascript `Date` as string. Type represents date and time as the ISO Date string.
scalar DateTime

type Mutation {
  addProfile(profile: ProfileInput!): Profile!
}

# Profile object.
type Profile {
  id: ID!

  # The name of the profile owner.
  name: String!

  # The email address of the profile owner.
  email: String!

  # The phone number of the profile owner.
  phone: String!

  # The url of where the profile image is stored on the cloud
  imageUrl: String!

  # The url of where the CV is stored on the cloud.
  cvUrl: String!

  # The date the profile was created
  createdAt: DateTime!

  # The date the profile was last updated
  updatedAt: DateTime!
  owner: User
}

input ProfileInput {
  # The name of the profile owner.
  name: String!

  # The email of the profile.
  email: String!

  # The phone number of the profile.
  phone: String

  # The url of where profile image is stored on the cloud.
  imageUrl: String

  # The url of where CV is stored on the cloud.
  cvUrl: String
}

type Query {
  profiles: [Profile!]!
  users: [User!]!
}

# User object.
type User {
  id: ID!

  # The first name of the user.
  firstName: String!

  # The last name of the user.
  lastName: String!

  # The email of the user.
  email: String!

  # A list of profiles which belong to the user.
  profiles: [Profile!]!
}
